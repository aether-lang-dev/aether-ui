# Build artifacts
build/
*.o
*.so
*.dylib
*.dll
*.exe

# System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~
aevg/test/screenshots/
aevg/_gen_*.ae
build/_gen_*
__pycache__/

# Scratch planning doc — local working notes, never committed
current_plan_dont_check_this_in.md

# LisMusic writes its SQLite history to history.db in the cwd (the repo root
# under ci.sh / manual runs). Runtime artifact — never commit it.
history.db

# aeb build output
target/

# aeb's generated SDK module farm — re-creatable, machine-specific, never source.
.aeb/
