import bldr
import aether
import aether (source, output, no_closure_regen)
import build_support.aetherui (ui_backend)
main() {
    bldr.build() {
        root = _get("root")
        aether.program() {
            source("wshortcut_demo.ae")
            output("wshortcut_demo")
            no_closure_regen()
            ui_backend(root)
        }
        return 0
    }
}
