import build
import aether
import aether (source, output, no_closure_regen, lib)
import build_support.aetherui (ui_backend)

main() {
    b = build.start()
    root = build._get(b, "root")
    aether.program(b) {
        source("frames_demo.ae")
        output("frames_demo")
        lib("${root}")
        no_closure_regen()
        ui_backend(root)
    }
    return 0
}
